import {PickerInputOptions} from '../types';
import {DropdownTrigger} from '@zui/dropdown/src/component/dropdown-trigger';

export class DropdownButton extends DropdownTrigger<PickerInputOptions> {
    
}