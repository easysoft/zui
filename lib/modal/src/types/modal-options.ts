export type ModalOptions = {
    position?: 'fit' | 'center' | 0,
    size?: 'sm' | 'lg' | 'fullscreen',
    moveable?: boolean,
    rememberPos?: boolean,
    fade?: boolean,
    scrollInsidei?: boolean
};
