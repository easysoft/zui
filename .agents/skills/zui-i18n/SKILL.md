---
name: zui-i18n
description: "为 ZUI 主仓库 lib/* 添加、补全或修复国际化文案、语言接线及覆盖和回退行为。需求明确时直接实施。"
---

# ZUI 国际化

## 工作流

1. 按 [共享工作流](../zui-standards/references/workflow.md) 定位目标、检查已有改动并复用上下文；按其澄清规则消除影响本次交付的歧义，需求明确时直接实施。
2. 按需阅读 [i18n 规范](../zui-standards/references/i18n.md) 中与本次变更有关的部分。
3. 阅读本次涉及的语言键、占位符及消费路径；新增语言接线或改变覆盖/回退行为时，再检查相关入口、类型和基础设施。
4. 新增或调整语言接线时，判断使用组件静态 `i18n` 映射，还是使用带命名空间的全局 `i18n.addLang`/当前仓库等价 API；不要把私有文案无理由放入全局命名空间。
5. 新接入时默认实现 `zh_cn`、`zh_tw`、`en`；局部修订保留既有结构与语言范围，以 `typeof zh_cn` 或等价类型约束键结构。确保入口或消费组件真实加载语言文件。
6. 使用 `{0}` 或 `{name}` 占位符，保留运行时覆盖参数；本次涉及语言解析或回退时，核对语言码归一化、缺失键回退和默认值行为。
7. 只修改国际化所需源码、类型与接线；文档、调试页和无关 API 仅在请求或共享批准范围明确包含时修改。
8. 按共享工作流验证本次涉及的键、占位符、类型或语言接线，修复范围内问题并复验；改变加载、覆盖或回退行为时补充对应消费路径检查，汇报结果，不自动提交。

## 组合边界

- 独立调用且目标明确时不增加计划确认门禁。
- 作为子流程时只处理共享范围内的本领域工作；上下文、批准和增量范围统一遵循共享工作流。
- 用户只要求审阅时保持只读。
