const colors = require('./colors');
const borderRadius = require('./border-radius');

module.exports = {
    DEFAULT: {
        color: colors,
        radius: borderRadius,
        'root-font-size': '16px',
    },
};
